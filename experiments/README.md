# 실험 기록 (emotion-bot)

| 파일 | 용도 |
|------|------|
| `yolo_runs.jsonl` | YOLO 학습 run (파라미터·메트릭) |
| `mtl_runs.jsonl` | (예정) MTL/CNN |
| `../configs/yolo/*.yaml` | 설정 스냅샷 |

- **전체 이력** → 이 폴더의 `*.jsonl` (Git 커밋)
- **채택 설정 요약** → 루트 `README.md` (확정 후만)
